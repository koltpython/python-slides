# <condition> is how we validate user input
# Take initial value
user_input = input([prompt])
# Check the condition and ask until its valid
while not <condition>:
    user_input = input([prompt])
# We know user_input passes the condition here
